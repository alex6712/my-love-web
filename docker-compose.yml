services:
  my-love-web:
    container_name: my-love-web
    build: .
    ports:
      - "80:80"
    networks:
      - app-network
    restart: unless-stopped


networks:
  app-network:
    driver: bridge
